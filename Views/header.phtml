<?php 
use WEP\Controller\UserController;
?>

<!DOCTYPE html>
<html>
    <head>
        <title>Mitgliederverwaltung</title>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="CSS/style.css" rel="stylesheet" type="text/css" />
        <link href="CSS/style_grid.css" rel="stylesheet" type="text/css"/>
    </head>
    <!-- header implementierung von moritz schüppel -->
        <nav id="header-nav">
			<div class="container" id="desktop-nav">
				<div class="row">
					<div class="col-6">
						<a href="?controller=User&do=showStart" class="logo-link">
							<img src="./img/logo.png" alt="logo">
						</a>

						<ul>
                            <li><a href="?controller=User&do=showStart">Willkommen</a></li>

                            <!-- header logik von timothy görzen -->
                            <li><a href="?controller=User&do=showProfile"> 
                                <?php if ($_SESSION["login"] == 1): ?>
                                    <?php print $_SESSION["username"] . "`s Profil"; ?>
                                    <?php endif ?>
                                </a></li>

                            <li><a href="?controller=User&do=showMitarbeiterProfil"> 
                            <?php if ($_SESSION["login"] == 1): ?>
                                <?php if ($_SESSION["isClerk"] == 1): ?>
                                    <?php print "Mitglieder"; ?>
                                <?php endif ?>
                                <?php endif ?>
                            </a></li>

                            <li><a href="?controller=User&do=showLogout">
                                <?php if ($_SESSION["login"] == 1): ?>
                                    <?php print "Abmelden"; ?>
                                <?php endif ?>
                                </a></li>

						</ul>
					</div>
				</div>
			</div>
		</nav>
    <main>